class Login:
    def __init__(self, id, username, password) -> None:
        self.id = id
        self.username = username
        self.password = password

        # create method to check if account credentials match with users db

    # def check_credentials()
